# ========================================
# 1. API Framework & Utilities (FastAPI Core)
# ========================================
fastapi==0.111.0
uvicorn[standard]>=0.30.1
pydantic>=2.7.1
pydantic-settings>=2.2.1
python-multipart>=0.0.9    # Untuk handle upload file (PDF/Image)
httpx>=0.27.0              # Async HTTP client

# ========================================
# 2. Database, ORM & Vector Search
# ========================================
SQLAlchemy>=2.0.30
asyncpg>=0.29.0            # Driver Async untuk PostgreSQL (Neon Tech)
alembic>=1.13.1            # Untuk migrasi / update tabel database
pgvector>=0.2.5            # Wajib: Ekstensi Vector DB untuk PostgreSQL

# ========================================
# 3. LLM Clients, LangGraph & RAG
# ========================================
langgraph>=0.0.60
langchain-core>=0.2.10
langchain-openai>=0.1.17       # Eksekutor Qwen via OpenAI API Format
langchain-google-genai>=1.0.6 
langchain-groq>=0.2.1
openai>=1.35.0                 # SDK Universal (dipakai untuk Qwen)
google-generativeai>=0.7.0     # Native SDK Gemini untuk Vision/Image
tiktoken>=0.7.0                # OpenAI Tokenizer (standar RAG)

# ========================================
# 4. Document Processing (PDF, Excel, Word)
# ========================================
pymupdf>=1.24.5            # Sangat cepat untuk baca PDF (pengganti pypdf)
python-docx>=1.1.2         # Baca file Word
openpyxl>=3.1.2            # Baca & Tulis file Excel (RAB)
pandas>=2.2.2              # Untuk manipulasi data Matrix MAUT
cloudinary>=1.40.0         # SDK Cloudinary untuk upload PDF Invoice
reportlab>=4.0.0           # Render PDF Invoice di dalam memori (BytesIO)

# ========================================
# 5. Auth & Security
# ========================================
python-jose[cryptography]>=3.3.0  # JWT Token
passlib[bcrypt]>=1.7.4            # Hashing Password

# ========================================
# 6. Background Tasks, Cache & WebSockets
# ========================================
redis>=5.0.4               # Klien Redis untuk Cache & Pub/Sub
celery>=5.4.0              # Antrean tugas (Task Queue) untuk OCR berat
websockets>=12.0           # Komunikasi Real-time (Chat & Log Agent)
apscheduler>=3.10.4
mlxtend>=0.23.1